<ul class="media-list">
  <% idx = 0; %>
  <%= while idx < object.comments.count do%>
    <li class="media">
      <a href="#" class="pull-left">
        <%= gravatar_for(object.comments[idx].user) %>
      </a>
      <div class="media-body">
        <span class="text-muted pull-right">
          <small class="text-muted">
            <%= time_ago_in_words(object.comments[idx].updated_at) %> ago
          </small>
        </span>
        <strong class="text-success">
          <%= object.comments[idx].user.username %>
        </strong>
        <p>
          <%= object.comments[idx].body %>
        </p>
      </div>
    </li>
    <% idx+=1 %>
  <% end %>
</ul>